import { lazy } from 'react';
import Layout from '../layout/User/Layout';
const routes = [
    {
        path: '/',
        element: <Layout></Layout>,
        children: []
    }
]

export { routes };
